#pragma once

namespace Maps {
    namespace Types {
        enum class Type {
            Corridor,
            Crossroad,
            
            RockCave,
            IceCave,
            LavaCave,
            LushCave,
            UnderwaterCave // (need air tank)
        };
    }
}